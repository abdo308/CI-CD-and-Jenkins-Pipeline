services:
  calculator:
    build: ./services/calculator
    environment:
      PORT: "8080"
    ports:
      - "8080:8080"

  frontend:
    build: ./frontend
    environment:
      VITE_API_URL: "http://localhost:8080"
    depends_on:
      - calculator
    ports:
      - "5173:5173"
